<view class="top-bar">
  <view><view class="title">{{title}}</view><view wx:if="{{subtitle}}" class="subtitle">{{subtitle}}</view></view>
  <button class="avatar" aria-label="打开设置" bindtap="openSettings">{{avatar || '我'}}</button>
</view>
